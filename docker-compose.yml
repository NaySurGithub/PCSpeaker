services:
  pcspeak-backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - AUTH_TOKEN=change-me-in-production
      - PORT=8080
      - LOG_LEVEL=info
    restart: unless-stopped
